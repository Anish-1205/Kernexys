# Kernexys

Kernexys is a local-first AI infrastructure control plane for coordinating AI workloads, tooling, and operational context without depending on remote infrastructure.

This project is intentionally minimal and dependency-free at the start, so it can evolve into a focused local control plane for AI workflows.
