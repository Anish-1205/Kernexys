apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kernexys-control-api-role
  labels:
    app.kubernetes.io/part-of: kernexys
rules:
  - apiGroups:
      - platform.kernexys.io
    resources:
      - modeldeployments
    verbs:
      - delete
      - get
      - patch
